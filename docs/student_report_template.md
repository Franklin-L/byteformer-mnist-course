# ByteFormer 微调 MNIST 实验报告

> 请填写自己的真实运行记录。无法完成的部分写明原因，不要用教师截图或同学结果代替。

## 1. 基本信息

- 姓名 / 学号：
- 日期：
- 环境：Kaggle GPU / AutoDL GPU / 本地 CPU / 其他（写明）：
- Python 版本：
- PyTorch 版本：
- GPU 名称（没有则写 CPU）：
- 课程代码版本（Git 提交号或教师课程包版本）：
- 我完成的路线：完整基线与对比 / 仅 CPU 流程验证 / 部分完成（写明）：

## 2. 我理解的实验

请用自己的话，每题回答 1–3 句话。

1. MNIST 的输入是什么，模型要输出什么？
2. 本实验中 ByteFormer 实际读取的是像素矩阵，还是 JPEG 文件字节？流程是怎样的？
3. “微调”与“从随机参数开始训练”有什么区别？本实验使用了哪种方式？
4. 训练集、验证集、测试集分别有什么用途？为什么不能反复根据测试准确率调整参数？
5. `loss` 与 `accuracy` 各自表示什么？
6. 官方训练部分有 60,000 张，本实验实际用多少张训练、多少张验证、多少张保留未用？测试图来自哪里？

## 3. 基线实验

正式设置：50,000 张训练、1,000 张验证、10,000 张独立测试；训练轮数自行设置并记录。官方训练部分另有 9,000 张验证池样本保留未用。

### 实际命令

把自己真正执行的训练命令粘贴到这里：

```bash
# 粘贴命令，不要把本行当作实验记录。
```

| 项目 | 自己的记录 |
| --- | --- |
| 使用的官方预训练文件 / 下载与校验结果 | |
| 训练 / 验证 / 测试样本数 | |
| epochs / batch size / 初始 learning rate | |
| 学习率衰减轮次 / 倍率（默认 4、6 / 0.2） | |
| 输出目录 | |
| 运行耗时（如有记录） | |
| 最佳验证轮次 | |
| 最佳验证准确率 | |
| 最终测试准确率 | |
| 指标单位（0–1 或百分比） | |

请插入 `curves.png`、`predictions.png` 和 `confusion_matrix.png`，或者给出所附文件名。

观察：训练损失总体如何变化？验证准确率总体如何变化？有没有明显波动？请依据图和数据回答。

## 4. 自选训练轮数的对比

两次实验均从同一官方预训练权重开始，样本、随机种子、批次大小、初始学习率和衰减设置保持相同。只改变总训练轮数，并分别保存结果。

```bash
# 粘贴第二次实验实际执行的命令。
```

| 项目 | 基线实验 | 自选对比实验 |
| --- | --- | --- |
| 实际训练轮数 | | |
| 输出目录 | | |
| 训练 / 验证 / 测试样本数 | | |
| batch size / 初始 learning rate | | |
| 学习率衰减轮次 / 倍率 | | |
| 最佳验证轮次 | | |
| 最佳验证准确率（统一单位） | | |
| 最后一轮训练损失 | | |
| 最终测试准确率（只作最终报告） | | |
| 运行耗时（如有） | | |

请插入第二组学习曲线。

- 根据**验证集**，另一设置是否改善了结果？用具体数据说明：
- 多训练几轮有什么收益或代价？
- 能否仅凭这两次结果断言“轮数越多越好”？为什么？
- 两次设置是否完全一致？如果调整过 batch size 或样本数，说明原因及其对比较的影响：

仅完成 CPU 快速路线的同学：明确标注本节未完成，并说明已经运行的实际参数。不要把 1 轮 CPU 结果改写成 完整基线结果。

## 5. 一个错例

- 样本来源：官方 MNIST 测试集 / 自制图片 / 其他：
- 测试集索引或图片文件名：
- 真实标签：
- 预测标签：
- 使用的 checkpoint 路径：
- 图像或终端输出证据：

这个数字有哪些容易混淆的笔画？你认为模型可能为什么出错？你的解释属于观察、推测还是经过额外实验验证的结论？

如果没有找到错误，请真实说明查找范围，并分析一个容易混淆的样本；不要伪造预测。

## 6. 问题与解决

| 遇到的问题或错误信息 | 我采取的操作 | 是否解决 / 证据 |
| --- | --- | --- |
| | | |

## 7. 提交检查

- [ ] 记录了实际环境、数据量和参数。
- [ ] 指标来自自己的 `metrics.json` 与 `history.csv`，验证和测试没有混写。
- [ ] 两次实验分别保存在不同目录，原始结果未被覆盖。
- [ ] 附上要求的曲线、预测图、混淆矩阵和错例证据。
- [ ] 没有提交 `.venv/`、整份数据集或不需要的大型模型文件。
- [ ] 写明了未完成的部分和使用的外部帮助。
