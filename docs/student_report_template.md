# 码流图像分类实验报告

> 根据实验记录填写参数、指标和分析，并附相关结果文件。

## 1. 基本信息

- 姓名 / 学号：
- 日期：
- 环境：Kaggle GPU / AutoDL GPU / 本地 CPU / 其他（写明）：
- Python 版本：
- PyTorch 版本：
- GPU 名称（没有则写 CPU）：
- 课程代码版本（Git 提交号或教师课程包版本）：
- 我完成的路线：完整基线与对比 / 仅 CPU 流程验证 / 部分完成（写明）：

## 2. 实验原理

请用自己的话，每题回答 1–3 句话。

1. MNIST 的输入是什么，模型要输出什么？
2. 本实验中 ByteFormer 实际读取的是像素矩阵，还是 JPEG 文件字节？流程是怎样的？
3. “微调”与“从随机参数开始训练”有什么区别？本实验使用了哪种方式？
4. 训练集、验证集、测试集分别有什么用途？为什么不能反复根据测试准确率调整参数？
5. `loss` 与 `accuracy` 各自表示什么？
6. 本实验实际使用多少张训练、验证和测试图？三类数据分别怎样使用？

## 3. 基线实验

数据设置：5,000 张训练、1,000 张验证、1,000 张独立测试，每个类别数量相同；训练轮数和 batch size 自行设置并记录。

### 实际命令

填写训练命令：

```bash
# 填写训练命令
```

| 项目 | 自己的记录 |
| --- | --- |
| 使用的官方预训练文件 / 下载与校验结果 | |
| 训练 / 验证 / 测试样本数 | |
| epochs / batch size / 初始 learning rate | |
| 学习率及调整设置 | |
| 输出目录 | |
| 运行耗时（如有记录） | |
| 最佳验证轮次 | |
| 最佳验证准确率 | |
| 最终测试准确率 | |
| 指标单位（0–1 或百分比） | |

请插入 `curves.png`、测试结果图和单图预测结果，或者给出所附文件名。

观察：训练损失总体如何变化？验证准确率总体如何变化？有没有明显波动？请依据图和数据回答。

## 4. 自选训练参数的对比

训练轮数和 batch size 均自行设置，记录具体数值与选择依据。建议一次只改变其中一项；保持数据划分、随机种子、预训练来源和学习率策略相同。分别保存结果；如果两项都变了，说明比较的局限。

```bash
# 粘贴第二次实验实际执行的命令。
```

| 项目 | 基线实验 | 自选对比实验 |
| --- | --- | --- |
| 实际训练轮数 | | |
| 输出目录 | | |
| 训练 / 验证 / 测试样本数 | | |
| batch size / 初始 learning rate | | |
| 学习率及调整设置 | | |
| 最佳验证轮次 | | |
| 最佳验证准确率（统一单位） | | |
| 最后一轮训练损失 | | |
| 最终测试准确率（只作最终报告） | | |
| 运行耗时（如有） | | |

请插入第二组学习曲线。

- 根据**验证集**，另一设置是否改善了结果？用具体数据说明：
- 调整所选参数有什么收益或代价？
- 能否仅凭这两次结果断言“轮数越多越好”？为什么？
- 两次改变了哪些参数？为什么这样设置？如果多项同时改变，如何影响比较：

如仅完成部分实验，请说明已完成的内容与后续计划。

## 5. 一个错例

- 样本来源：官方 MNIST 测试集 / 自制图片 / 其他：
- 测试集索引或图片文件名：
- 真实标签：
- 预测标签：
- 使用的 checkpoint 路径：
- 图像或终端输出证据：

这个数字有哪些容易混淆的笔画？你认为模型可能为什么出错？你的解释属于观察、推测还是经过额外实验验证的结论？

若没有找到错例，说明查找范围，并分析一个容易混淆的样本。

## 6. 加分项（选做）

若完成损坏码流分类，请填写 Clean、Medium-Flip、Medium-Loss 三类结果，并说明 bit flip 与 byte loss 的区别、所用改进方法及其效果。

| 测试场景 | 基础模型 | 改进模型 |
| --- | --- | --- |
| Clean | | |
| Medium-Flip | | |
| Medium-Loss | | |

改进方法：损坏增强：

## 7. 问题与解决

| 遇到的问题或错误信息 | 我采取的操作 | 是否解决 / 证据 |
| --- | --- | --- |
| | | |

## 8. 最终提交清单

请将文件整理为 `学号_姓名_码流图像分类.zip`。基础任务必须包含：

- [ ] `实验报告.pdf`（或教师指定格式的报告文件）。
- [ ] 基础模型权重 `course_clean/best.pt`。
- [ ] `course_clean/metrics.json`、`history.csv`、`curves.png` 和 `prediction_single.png`。
- [ ] `course_clean_eval/evaluation.json`、`predictions.png` 和 `confusion_matrix.png`。
- [ ] `comparison/metrics.json`、`history.csv` 和 `curves.png`。
- [ ] 报告中填写实际环境、数据划分、参数、命令、验证结果、测试结果、参数对比和错例。

如果完成加分项，再加入自己完成的增强模型和评估结果：

- [ ] 加分模型的 `best.pt`。
- [ ] 加分模型评估目录中的 `evaluation.json`、`accuracy.csv` 和 `accuracy.png`。
- [ ] 报告中填写 Clean、Medium-Flip、Medium-Loss 三类准确率和所用方法。

不要提交：

- [ ] 整份 MNIST 数据集、`.venv/`、完整 GitHub 仓库或课程 PPT。

- [ ] 所有指标来自自己的输出文件，验证集和测试集没有混写。
- [ ] 如果有未完成项目，在报告中直接注明。
